# Standup – 2025-03-12

[DE]
1. Der LDAP-Dev-Server wurde mit dem physischen Hostnamen konfiguriert, aber das SSL-Zertifikat gilt nur für den DNS-Alias. PHP lehnt die Verbindung wegen eines TLS-Hostname-Mismatches ab — der Server selbst läuft einwandfrei.
2. Ich habe mit Patrick Krüger über das SOAP-Service-Testen gesprochen — das, was unklar war, wird nicht mehr verwendet.
3. Für den test-orca-Service warte ich, bis Mark Kühn von der Krankschreibung zurückkehrt.
4. Ich arbeite weiter an DWHWEB-8040.

[SR]
1. LDAP dev server je konfigurisan sa fizičkim hostname-om, ali SSL sertifikat važi samo za DNS alias. PHP odbija konekciju zbog TLS hostname mismatch-a — sam server radi ispravno.
2. Razgovarao sam sa Patrick Krüger o testiranju SOAP servisa — ono što je bilo upitno više nije u upotrebi.
3. Za test-orca servis čekam da se Mark Kühn vrati sa bolovanja.
4. Nastavljam da radim na DWHWEB-8040.

[EN]
1. The LDAP dev server was configured with the physical hostname, but the SSL certificate is only valid for the DNS alias. PHP rejects the connection due to a TLS hostname mismatch — the server itself runs fine.
2. I talked with Patrick Krüger about SOAP service testing — what was unclear is no longer in use.
3. For the test-orca service I'm waiting for Mark Kühn to return from sick leave.
4. I'm continuing to work on DWHWEB-8040.
